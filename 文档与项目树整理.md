
# 项目 Agent 导航层与大文件治理执行要求

## 目标

当前项目过大，导致每次开发时 agent 都需要重新理解目录结构、模块边界、入口文件、测试方式和风险区域，消耗大量 token 和时间。

本次任务的目标是建立一套**面向 agent 工作流的项目导航层**，并对明显过大的文件进行治理，使后续 agent 可以：

1. 先读少量文档理解项目结构。
2. 根据任务快速定位相关模块。
3. 避免无意义地全文扫描整个项目。
4. 知道每类改动应该看哪些文件、跑哪些验证。
5. 在后续开发中持续维护这套导航层。

本次任务不是为了重写业务逻辑，也不是大规模架构重构。重点是**降低理解成本、明确边界、建立可维护文档入口**。

---

## 必须遵守的项目要求

项目根目录已有要求：

```text
Always update docs\DIRECTORY_REFERENCE.md file.

完整地完成任务，绝不交付半成品：如果用户提出一个要求，不要只做了一部分，然后告诉用户下一步可以做xxx，而是把任务全部完成，直到没有下一步。

在任务收尾阶段，拆分任务中产生的大文件。
```

因此本任务必须做到：

1. 必须创建或更新 `docs\DIRECTORY_REFERENCE.md`。
2. 不能只给建议，必须实际完成文档建设。
3. 任务结束前检查本次新增或修改的大文件。
4. 如果本次产生了过大的文档，必须拆分。
5. 不允许留下“以后可以补充”的空白章节。
6. 不允许写泛泛而谈的文档，必须结合当前项目真实目录和文件。

---

## 总体执行原则

### 1. 先建立导航层，再考虑拆分代码

不要一上来重构代码。项目过大时，直接重构风险很高，也会消耗大量上下文。

优先顺序是：

1. 扫描项目目录。
2. 理解主要模块。
3. 更新 `docs\DIRECTORY_REFERENCE.md`。
4. 创建必要的模块文档。
5. 创建开发/测试/构建说明。
6. 标记大文件和高风险区域。
7. 只在低风险且边界清晰时拆分文件。
8. 最后验证文档和目录引用是否准确。

原因：  
导航层可以立刻降低后续 agent 的理解成本；代码拆分如果没有模块边界文档，容易拆错。

---

## 推荐文档结构

请在 `docs` 下建立或整理如下结构：

```text
docs\
  DIRECTORY_REFERENCE.md
  architecture\
    overview.md
    data-flow.md
    module-boundaries.md
  development\
    build-and-test.md
    agent-workflow.md
    large-file-policy.md
  modules\
    <module-name>.md
```

如果项目已有类似文档，应优先复用和更新，不要重复创建同义文档。

---

## 一、必须更新 `docs\DIRECTORY_REFERENCE.md`

这是最重要的文件。它是所有 agent 进入项目后的第一入口。

### 内容要求

`docs\DIRECTORY_REFERENCE.md` 必须包含以下章节：

```md
# Directory Reference

## Purpose

说明这个文件的作用：帮助开发者和 agent 快速理解项目结构，避免每次从零扫描整个仓库。

## How Agents Should Use This Project

写清楚 agent 的推荐工作流：

1. 先读本文件。
2. 根据任务类型定位目录。
3. 阅读对应模块文档。
4. 只搜索相关目录。
5. 修改结构后必须更新本文件。

## Root Directory

逐项说明根目录下的重要文件和目录。

例如：
- `src\`: 主程序源码。
- `docs\`: 项目文档。
- `tests\`: 测试代码。
- `scripts\`: 脚本。
- `README.md`: 项目入口说明。

必须根据真实项目填写，不能照抄示例。

## Directory Map

列出主要目录树，但不要贴完整无差别文件树。

只列重要目录和关键文件。

## Module Guide

对每个核心模块说明：

- 职责
- 入口文件
- 常见修改场景
- 相关测试
- 相关文档
- 风险提示

## Common Task Routing

按任务类型告诉 agent 应该看哪里。

例如：
- 修改上传逻辑：先看 A、B、C。
- 修改配置加载：先看 D、E。
- 修改 UI：先看 F。
- 修改构建流程：先看 G。

## Generated / Low Priority / Do Not Start Here

列出不建议 agent 首先阅读的内容，例如：

- 构建产物
- 缓存目录
- 第三方依赖
- 自动生成文件
- 历史兼容文件
- 大型静态资源

## Validation Commands

列出可用的验证命令：

- 安装依赖
- 构建
- 单元测试
- 类型检查
- lint
- 运行项目

如果某些命令不存在，也要说明“未发现”。

## Maintenance Rules

说明以后什么时候必须更新本文件：

- 新增目录
- 移动文件
- 拆分模块
- 新增测试目录
- 新增构建脚本
- 修改入口文件
```

### 质量要求

`DIRECTORY_REFERENCE.md` 不能只是目录列表。它必须回答：

- 这个目录是干什么的？
- 哪些文件是入口？
- 哪些文件通常不用先看？
- 改某类功能应该从哪里开始？
- 改完怎么验证？

原因：  
单纯目录树对 agent 帮助有限。agent 需要的是“任务路由图”。

---

## 二、创建模块级文档

对每个主要模块，在 `docs\modules\` 下创建对应文档。

文件名建议使用英文或现有模块名，例如：

```text
docs\modules\transport.md
docs\modules\config.md
docs\modules\ui.md
docs\modules\storage.md
```

具体模块名必须基于真实项目目录决定。

### 每个模块文档必须包含

```md
# Module: <模块名>

## Responsibility

说明模块职责。

## Key Files

列出关键文件，并说明作用。

## Entry Points

说明外部通常从哪里调用这个模块。

## Data Flow

说明数据如何进入、处理、输出。

## Important Types / Classes / Functions

列出核心类型、类、函数。

## Common Change Scenarios

按常见开发任务说明应该改哪里。

## Tests

列出相关测试文件或说明暂未发现测试。

## Risks

说明修改这个模块时容易出错的地方。

## Related Docs

链接到其他相关文档。
```

### 模块文档不要求逐行解释代码

不要把代码复制进文档。  
重点是帮助 agent 快速决定“应该读哪些源文件”。

原因：  
如果模块文档过细，会变成另一份需要维护的代码副本，后续容易过期。

---

## 三、创建架构概览文档

创建或更新：

```text
docs\architecture\overview.md
```

### 内容要求

必须包含：

1. 项目的整体用途。
2. 主要运行流程。
3. 核心模块之间的关系。
4. 入口层、业务层、存储层、网络层、UI 层等边界。
5. 当前架构中的高风险区域。
6. 不建议随意修改的区域。

可以使用 Mermaid 图，但不是必须。

示例结构：

```md
# Architecture Overview

## System Purpose

## Main Runtime Flow

## Major Components

## Dependency Direction

## High-Risk Areas

## Known Constraints
```

原因：  
`DIRECTORY_REFERENCE.md` 解决“去哪看”，`overview.md` 解决“整体怎么运转”。

---

## 四、创建模块边界文档

创建或更新：

```text
docs\architecture\module-boundaries.md
```

### 内容要求

说明模块之间允许和不允许的依赖关系。

例如：

```md
# Module Boundaries

## Allowed Dependencies

- UI 可以调用 service。
- service 可以调用 storage。
- storage 不应反向依赖 UI。

## Disallowed Dependencies

- 底层工具模块不应 import UI。
- 配置模块不应依赖业务流程。
- 测试辅助代码不应进入生产路径。

## Boundary Rules for Future Agents

修改跨模块逻辑时，必须先确认调用方向。
```

原因：  
大型项目的 token 消耗通常来自边界不清。边界不清时，agent 会到处搜索、到处改。

---

## 五、创建开发验证文档

创建或更新：

```text
docs\development\build-and-test.md
```

### 内容要求

记录当前项目真实可用的命令。

必须通过读取项目文件确认，例如：

- `package.json`
- `pyproject.toml`
- `requirements.txt`
- `Cargo.toml`
- `go.mod`
- `Makefile`
- `.github\workflows`
- 其他构建脚本

文档应包含：

```md
# Build and Test

## Environment

说明运行环境、语言、包管理器。

## Install

安装依赖命令。

## Run

本地运行命令。

## Build

构建命令。

## Test

测试命令。

## Lint / Type Check

lint 或类型检查命令。

## Notes

已发现的问题、缺失的脚本、平台注意事项。
```

如果命令无法确认，不要编造。写明“未在项目中发现明确命令”。

原因：  
后续 agent 改完代码后必须知道如何验证，否则容易交付未验证结果。

---

## 六、创建 Agent 工作流文档

创建：

```text
docs\development\agent-workflow.md
```

### 内容要求

这个文件专门告诉后续 agent 如何低成本进入项目。

建议内容：

```md
# Agent Workflow

## First Files to Read

1. `docs\DIRECTORY_REFERENCE.md`
2. `docs\architecture\overview.md`
3. 与任务相关的 `docs\modules\*.md`

## Search Strategy

- 优先在相关模块目录搜索。
- 不要一开始全仓库扫描。
- 搜索符号时使用 ripgrep。
- 避免阅读生成文件、依赖目录和构建产物。

## Before Editing

- 确认模块边界。
- 确认入口文件。
- 确认测试位置。
- 检查是否需要更新 `docs\DIRECTORY_REFERENCE.md`。

## After Editing

- 更新相关文档。
- 运行相关验证。
- 检查是否产生大文件。
```

原因：  
这个文件可以直接改变后续 agent 的行为，减少重复理解成本。

---

## 七、创建大文件治理文档

创建：

```text
docs\development\large-file-policy.md
```

### 内容要求

说明项目中如何判断和处理大文件。

建议规则：

```md
# Large File Policy

## Why This Exists

大文件会增加 agent 阅读成本，也会增加修改风险。

## Thresholds

- 超过 400 行：需要关注。
- 超过 800 行：建议拆分。
- 超过 1200 行：必须评估拆分。
- 文档超过 300 行：优先拆成多个专题文档。

## Preferred Splitting Strategies

### Source Code

- 按职责拆分。
- 按入口、业务逻辑、工具函数拆分。
- 按 UI 容器、状态逻辑、子组件拆分。
- 避免为了拆分而改变行为。

### Documentation

- 总览文件只保留导航。
- 详细内容移动到专题文件。
- 使用相对链接连接文档。

## Rules

- 拆分前必须理解文件职责。
- 拆分后必须保持导入路径正确。
- 拆分后必须运行相关验证。
- 不能把一个大文件机械拆成多个无意义小文件。
```

原因：  
大文件治理需要长期执行，不能只靠一次人工判断。

---

## 八、扫描并记录大文件

必须扫描项目中的大文件，并在文档中记录。

可以创建：

```text
docs\development\large-file-inventory.md
```

也可以把摘要放进 `large-file-policy.md`，如果内容不多。

### 内容要求

列出：

- 文件路径
- 行数
- 文件类型
- 所属模块
- 是否建议拆分
- 拆分建议
- 风险等级

示例：

```md
# Large File Inventory

| File | Lines | Module | Risk | Recommendation |
|---|---:|---|---|---|
| `src\xxx\main.ts` | 950 | UI | High | 拆分容器、状态、子组件 |
| `docs\xxx.md` | 420 | Docs | Medium | 拆成 overview 和 reference |
```

原因：  
这能让后续 agent 不用重新找大文件，也能给后续重构提供路线图。

---

## 九、代码大文件拆分要求

如果发现明显过大的代码文件，是否立即拆分要谨慎。

### 可以立即拆分的情况

满足以下条件时可以拆：

1. 文件职责明显混杂。
2. 拆分后不改变业务行为。
3. 项目有测试或可运行验证。
4. 导入导出关系清晰。
5. 拆分范围局限在一个模块内。

### 不应立即拆分的情况

以下情况不要强行拆：

1. 文件虽然大，但业务流程高度耦合。
2. 缺少测试，且运行方式不明确。
3. 涉及多个模块公共契约。
4. 拆分会影响大量 import。
5. agent 无法确认行为是否保持一致。

如果不拆代码，也必须在 `large-file-inventory.md` 里写清楚拆分建议和原因。

原因：  
本任务的核心是降低理解成本，不是制造高风险重构。

---

## 十、文档大文件必须拆分

如果本任务中新建或更新的文档过大，必须拆分。

建议标准：

- 单个文档超过 300 行，考虑拆分。
- 单个文档超过 500 行，必须拆分。
- `DIRECTORY_REFERENCE.md` 不应变成百科全书。
- `DIRECTORY_REFERENCE.md` 只保留总导航，把详细内容链接到其他文档。

原因：  
如果导航文档本身过大，后续 agent 仍然会浪费 token。

---

## 十一、链接和路径要求

所有文档中的路径必须使用项目内真实路径。

要求：

1. Windows 路径可以使用 `\`，例如 `docs\DIRECTORY_REFERENCE.md`。
2. Markdown 链接建议使用相对路径。
3. 不允许链接不存在的文件。
4. 不允许保留空链接。
5. 不允许写“待补充”。

错误示例：

```md
详情见 TODO。
后续补充。
相关文件：src/unknown.ts
```

正确示例：

```md
相关文档：[Build and Test](development/build-and-test.md)
相关源码：`src\transport\client.ts`
```

---

## 十二、验证要求

任务结束前必须完成以下检查：

1. `docs\DIRECTORY_REFERENCE.md` 存在且已更新。
2. 所有新增文档都能从 `DIRECTORY_REFERENCE.md` 找到。
3. 所有文档中的路径真实存在。
4. 所有模块文档都有实际内容。
5. 没有“TODO”“待补充”“以后再说”等半成品表述。
6. 已记录大文件清单。
7. 本次产生的大文档已拆分。
8. 如果改了代码，必须运行相关测试或构建命令。
9. 如果无法运行验证，必须在最终说明中写明原因。

---

## 十三、建议执行步骤

请按以下顺序执行：

1. 读取项目根目录文件，确认技术栈。
2. 扫描主要目录，不要读取依赖目录和构建产物。
3. 查找已有文档，避免重复建设。
4. 识别源码入口、测试入口、构建入口。
5. 更新或创建 `docs\DIRECTORY_REFERENCE.md`。
6. 创建 `docs\architecture\overview.md`。
7. 创建 `docs\architecture\module-boundaries.md`。
8. 创建 `docs\development\build-and-test.md`。
9. 创建 `docs\development\agent-workflow.md`。
10. 创建 `docs\development\large-file-policy.md`。
11. 创建必要的 `docs\modules\*.md`。
12. 扫描并记录大文件清单。
13. 拆分本任务产生的过大文档。
14. 检查所有链接和路径。
15. 输出最终完成报告。

---

## 最终交付要求

最终回复必须包含：

1. 修改了哪些文件。
2. 新增了哪些文档。
3. `docs\DIRECTORY_REFERENCE.md` 如何使用。
4. 发现了哪些大文件。
5. 是否拆分了文件。
6. 运行了哪些验证。
7. 是否存在未验证项及原因。

不要只说“已完成文档整理”。必须说明实际产物。