世界树计划的当前任务是把模块生命周期、事件总线和导入链路接到正式持久化底座上。导入链路需要把文本切分为稳定片段，并生成可以落库的节点与来源注解。本阶段任务优先级最高。

导入链路的第二项任务是把相关段落之间的关系边写入数据库。模块生命周期、事件总线和检索链路必须共享一致的术语和来源规则，这是当前计划的核心目标。

当一次小型资料导入完成后，系统应该能够通过 API 返回节点内容、关联节点名称和来源摘要，并允许后续任务继续在这些临时节点之上整理与扩展。这也是本轮计划的验收目标。